<Page x:Class="Ffu.Master.TabMainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Ffu.Master"
      mc:Ignorable="d" 
      Height="700" Width="1250"
      Title="TabMainView">
    <Page.Resources>
        <SolidColorBrush x:Key="gray50" Color="#F7F8F9"/>
        <SolidColorBrush x:Key="gray100" Color="#E8EBED"/>
        <SolidColorBrush x:Key="gray200" Color="#C9CDD2"/>
        <SolidColorBrush x:Key="gray400" Color="#9EA4AA"/>
        <SolidColorBrush x:Key="gray500" Color="#72787F"/>
        <SolidColorBrush x:Key="gray600" Color="#454C53"/>
        <SolidColorBrush x:Key="gray800" Color="#26282B"/>
        <SolidColorBrush x:Key="gray900" Color="#1B1D1F"/>


    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
            <!-- 상단바 -->
        <Grid Grid.Row="0" DockPanel.Dock="Top" Background="{StaticResource gray600}">
            <Border BorderThickness="1" BorderBrush="#72787f">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center" Margin="10,0">
                        <TextBlock Text="FFU Ctrl system" Foreground="White" FontSize="16" FontWeight="Bold"/>
                    </StackPanel>

                    <!-- Tab-style buttons container -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top" Margin="10,0,0,0">

                        <!-- View Tab -->
                        <Border CornerRadius="8,8,0,0" Margin="0,5,15,0" BorderThickness="1,1,1,0" BorderBrush="#BDC3C7">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#95A5A6" Offset="0"/>
                                    <GradientStop Color="#7F8C8D" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Button Content="View" Foreground="White" FontWeight="SemiBold" Padding="115,8" BorderThickness="0" Background="Transparent" Click="BtnView_Click"/>
                        </Border>

                        <!-- Setting Tab -->
                        <Border CornerRadius="8,8,0,0" Margin="0,5,0,0" BorderThickness="1,1,1,0" BorderBrush="#BDC3C7">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#27AE60" Offset="0"/>
                                    <GradientStop Color="#1D8348" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Button Content="Setting" Foreground="White" FontWeight="SemiBold" Padding="115,8" BorderThickness="0" Background="Transparent" Click="BtnSetting_Click"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
        <!-- 본문 Frame -->
        <Grid Grid.Row="1">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" VerticalAlignment="Top"/>
        </Grid>
    </Grid>
</Page>
