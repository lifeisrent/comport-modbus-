<Page x:Class="Ffu.Master.TabMainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Ffu.Master"
      mc:Ignorable="d" 
      Height="700" Width="1250"
      Title="TabMainView">
    <Page.Resources>
        <SolidColorBrush x:Key="gray50" Color="#F7F8F9"/>
        <SolidColorBrush x:Key="gray100" Color="#E8EBED"/>
        <SolidColorBrush x:Key="gray200" Color="#C9CDD2"/>
        <SolidColorBrush x:Key="gray400" Color="#9EA4AA"/>
        <SolidColorBrush x:Key="gray500" Color="#72787F"/>
        <SolidColorBrush x:Key="gray600" Color="#454C53"/>
        <SolidColorBrush x:Key="gray800" Color="#26282B"/>
        <SolidColorBrush x:Key="gray900" Color="#1B1D1F"/>


    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
            <!-- 상단바 -->
            <Grid Grid.Row="0" DockPanel.Dock="Top" Background="{StaticResource gray600}" >
                <Border BorderThickness="1" BorderBrush="#72787f"  >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center" Margin="10,0">
                            <TextBlock Text="FFU Ctrl system" Foreground="White" FontSize="16" FontWeight="Bold"/>
                        </StackPanel>

                        <Button Grid.Column="1" Content="Setting" Foreground="White" Margin="5" Padding="10,5" BorderThickness="0" Click="BtnSetting_Click">

                            <Button.Background>

                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#27AE60" Offset="0"/>
                                    <GradientStop Color="#1D8348" Offset="1"/>
                                </LinearGradientBrush>

                            </Button.Background>

                        </Button>
                        <Button Grid.Column="2" Content="EXIT" Foreground="White" Margin="5,5,25,5" Padding="10,5" BorderThickness="0" Click="BtnExit_Click">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#E74C3C" Offset="0"/>
                                    <GradientStop Color="#A93226" Offset="1"/>
                                </LinearGradientBrush>
                            </Button.Background>
                        </Button>
                    </Grid>
                </Border>

            </Grid>

            <!-- 본문 Frame -->
        <Grid Grid.Row="1">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Page>
